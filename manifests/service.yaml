apiVersion: v1
kind: Service
metadata:
  name: Deployment
  namespace: namespace-workflow-bosch
spec:
  type: LoadBalancer
  selector:
    app: Deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
