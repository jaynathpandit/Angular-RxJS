apiVersion: v1
kind: Service
metadata:
  name: Deployment
  namespace: namespace-workflow-1693495803035
spec:
  type: LoadBalancer
  selector:
    app: Deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000